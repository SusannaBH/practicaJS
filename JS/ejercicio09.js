//Define un array con las letras ‘a’, ‘b’, ‘c’, ‘d’ y ‘e’. Recorre cada uno de los elementos mediante a forEach
const arr = ["a", "b", "c", "d", "e"];

arr.forEach(element => {
    console.log("foreach " + element);
});

//Define una variable let numero = 5; que itere hasta que el valor sea 0
for (let numero = 5; numero >= 0; numero--) {

    console.log("for " + numero);
};